name: Legacy GitHub Pages Deploy (Manual Only)

on:
  workflow_dispatch:

jobs:
  legacy-note:
    runs-on: ubuntu-latest
    steps:
      - name: Inform
        run: |
          echo "GitHub Pages deploy is deprecated for this app."
          echo "Primary deployment target is Vercel."
